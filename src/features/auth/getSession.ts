/**
 * Session serveur (Supabase) — placeholder tant que l'auth n'est pas branchée.
 * Phase 4 : implémenter avec `createServerClient` + cookies.
 */
export async function getSession(): Promise<{ userId: string } | null> {
  return null;
}
